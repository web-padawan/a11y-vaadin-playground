{
  "name": "a11y-vaadin-playground",
  "private": true,
  "author": "Vaadin Ltd",
  "license": "Apache-2.0",
  "scripts": {
    "dist": "rimraf dist && rollup -c rollup.config.js && rollup -c rollup.v21.config.js && rollup -c rollup.v22.config.js",
    "format": "prettier index.html tmp --write",
    "postinstall": "./scripts/install.sh",
    "start": "web-dev-server --node-resolve"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.22",
    "@web/rollup-plugin-html": "^1.10.0",
    "prettier": "^2.4.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.56.3",
    "rollup-plugin-terser": "^7.0.2"
  }
}
